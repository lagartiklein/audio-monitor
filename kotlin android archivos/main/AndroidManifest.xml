<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos generales -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- ✅ Permisos de audio -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Permisos específicos para Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"
        android:minSdkVersion="33" />

    <!-- ✅ Permisos para servicio en segundo plano y WiFi -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- Features de audio -->
    <uses-feature android:name="android.hardware.audio.low_latency" android:required="false" />
    <uses-feature android:name="android.hardware.audio.pro" android:required="false" />

    <!-- Soporte pantallas grandes -->
    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:anyDensity="true"
        android:resizeable="true"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logooficialdemo"
        android:label="Fichatech"
        android:roundIcon="@drawable/logooficialdemo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:targetSandboxVersion="2"
        android:resizeableActivity="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="36">

        <!-- Configuración de AdMob -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-5677027647580832~9356614563"/>
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version"/>

        <!-- ✅ SERVICIO DE STREAMING AUDIO (SEGUNDO PLANO) -->
        <service
            android:name=".audiostream.AudioStreamService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"
            android:enabled="true" />

        <!-- Actividades -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Fichatech.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".auth.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.Fichatech.Light"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".auth.VerificationPendingActivity"
            android:exported="false"
            android:theme="@style/Theme.Fichatech.Light" />

        <activity
            android:name=".auth.PrivacyPolicyActivity"
            android:exported="false"
            android:label="Política de Privacidad"
            android:theme="@style/Theme.Fichatech.Light" />

        <activity
            android:name=".WebViewActivity"
            android:exported="false"
            android:theme="@style/Theme.Fichatech.Light" />

        <activity
            android:name="com.cepalabsfree.fichatech.fichatecnica.CrearFichaActivity"
            android:exported="false"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true"
            android:configChanges="screenSize|screenLayout|density|keyboardHidden|smallestScreenSize|uiMode"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustResize|stateAlwaysHidden" />

        <activity
            android:name="com.cepalabsfree.fichatech.fichatecnica.ViewFichaActivity"
            android:exported="false"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true"
            android:configChanges="screenSize|screenLayout|density|keyboardHidden|smallestScreenSize|uiMode"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustResize|stateAlwaysHidden" />

        <activity
            android:name="com.cepalabsfree.fichatech.MainActivity"
            android:exported="true"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true"
            android:configChanges="screenSize|screenLayout|density|keyboardHidden|smallestScreenSize|uiMode"
            android:theme="@style/AppTheme"/>

        <!-- ✅ ACTIVITY DE AUDIO STREAM (ACTUALIZADA) -->
        <activity
            android:name="com.cepalabsfree.fichatech.audiostream.NativeAudioStreamActivity"
            android:exported="false"
            android:theme="@style/AppTheme"
            android:resizeableActivity="true"
            android:configChanges="screenSize|screenLayout|density|keyboardHidden|smallestScreenSize|uiMode|orientation|layoutDirection"
            android:windowSoftInputMode="adjustResize|stateAlwaysHidden" />

        <activity
            android:name="com.cepalabsfree.fichatech.BienvenidaActivity"
            android:exported="false"
            android:theme="@style/Theme.Fichatech.Light" />

        <!-- FileProvider para compartir PDFs -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

    </application>

</manifest>